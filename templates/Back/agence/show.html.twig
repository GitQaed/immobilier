{% extends 'base.html.twig' %}

{% block title %}Agence{% endblock %}

{% block body %}
    <h1>Agence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agence.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ agence.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('agence_index') }}">back to list</a>

    <a href="{{ path('agence_edit', {'id': agence.id}) }}">edit</a>

    {{ include('back/agence/_delete_form.html.twig') }}
{% endblock %}
